//! The migration ledger (pre-stub). Body lands in #272 -- the versioned,
//! chain-hashed, tamper-evident `_smugglr_migrations` table with a
//! `UNIQUE(version)` apply-winner and `success`-gated skip. Empty until then.
