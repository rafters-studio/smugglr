//! Forward apply engine (pre-stub). Body lands in #273 -- local-only ordered,
//! idempotent, per-op apply exposing `apply_ops`; remote targets are pure
//! statement generators (transport is #291). Empty until then.
