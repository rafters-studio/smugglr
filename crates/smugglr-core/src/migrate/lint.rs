//! The destructive-lint (pre-stub). Body lands in #275 -- classifies every op
//! (additive / destructive / hash-rewriting), gates pre-image capture, and warns.
//! Reuses `manifest::OpClass`. Empty until then.
