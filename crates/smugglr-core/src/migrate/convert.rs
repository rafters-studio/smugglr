//! `int -> UUIDv7` conversion migration (pre-stub). Body lands in #280 -- the
//! flagship onboarding conversion that rewrites every PK, cascades every FK, and
//! rewrites every content-hash. Gates #268's 0.5.x refuse-flip. Empty until then.
